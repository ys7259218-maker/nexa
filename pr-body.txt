## Changes
The signup form now shows a consent line with links to the Terms of Service and Privacy Policy, separated by a hairline from the rest of the card. Previously creating an account implied consent with no way to read the governing documents.

## Notes
- Display-only change; no auth logic touched.
- Uses the same muted link treatment and legal route targets as the LegalPage footer nav.
- Contract test asserts the consent copy and both legal destinations.

## Verification
- `npm run check` green: 284 tests + 4 issue-report tests, lint clean, TypeScript clean, production build compiles.