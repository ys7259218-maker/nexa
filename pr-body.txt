The `/notifications` route (linked from the header bell and sidebar) was a dead 404 — the loader and page are now real.

## What changes
- New `app/notifications/page.tsx` (auth-gated, read-only): renders `getNotifications()` derived from live workspace state (pending AI drafts, open conversations, unlinked AI employees), each with a priority tone (danger/warning/info), linked to the relevant area.
- New `app/notifications/loading.tsx` with an "Loading notifications" accessible label.
- This completes the previously-empty route so the header bell badge and the sidebar "Notifications" entry point somewhere real.

## Tests
- UI-contract assertions: page wiring (`getNotifications(client)`, tone classes, empty state, row links), and completion of the pre-existing "notifications are real" contract (page shell, loader, header badge, sidebar entry, dashboard wiring).
- `npm run check` green: 336 node tests + 4 issue-reports, lint/typecheck/build clean.