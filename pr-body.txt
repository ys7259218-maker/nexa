## Changes
The inbox's "Recent chats" label is now a real heading (`<h2>`) instead of a styled paragraph.

The conversation inbox is a two-pane screen: the message thread already had an `<h2>` (the contact name), but the list side was only a visually-styled `<p>` — invisible to heading-based screen-reader navigation. Screen-reader users can now jump straight to "Recent chats" as a proper section heading.

## Notes
- Code-only; the styling classes are identical, so there is no visual change.
- Contract test pins that "Recent chats" is an `<h2>`.

## Verification
- `npm run check` green: 296 tests + 4 issue-report tests, lint clean, TypeScript clean, production build compiles.