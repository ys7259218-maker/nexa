## Changes
The root landing page (`/`) was the last route with no page-specific `metadata` — it fell back to the root layout's generic "Nexa AI" title. This PR gives it a dedicated browser title and description:

- `title: "Welcome | Nexa AI"` — matches the page's visible "Welcome to Nexa" heading
- `description: "Let's build your first AI Employee."` — the onboarding tagline shown on the page

The contract test now includes `/` in its metadata-title sweep so new pages must declare their title.

## Verification
- `npm run check` green: 277 tests + 4 issue-report tests, lint clean, TypeScript clean, production build compiles.