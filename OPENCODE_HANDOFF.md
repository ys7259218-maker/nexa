# OpenCode handoff

Continue from this repository; do not scaffold a replacement project.

```bash
git clone https://github.com/ys7259218-maker/nexa.git
cd nexa
npm ci
copy .env.example .env.local
npm run check
```

Read `docs/NEXA_VISION_AND_SAFETY.md`, `docs/NEXA_MASTER_BLUEPRINT.md`, `NEXA_PROJECT_RECAP.md`, `README.md`, `docs/SUPABASE_SETUP.md`, and `docs/WHATSAPP.md` before editing. The vision and safety contract is non-negotiable, and the master blueprint defines the execution sequence. Preserve the current UI while replacing demo surfaces incrementally.

The active multi-tool work queue, branch rules, closed-beta gates, and first bounded OpenCode packet are in `docs/AI_EXECUTION_BOARD.md`. OpenCode must use its own branch, must not receive secrets or direct production access, and must not push directly to `main`.

Recommended next slice:

1. Done: Supabase Next.js SSR client pattern, `proxy.ts` session refresh, and server-side `requireAuthenticatedUser()` gates on `/dashboard`, `/dashboard/ai-employees/new`, `/ai-employees`, and `/ai-employees/[id]`.
2. Done: Consolidated create/list flows around the typed data layer in `lib/aiEmployees.ts` (list/get/create/update/delete under RLS); `/ai-employees` lists real records and `/ai-employees/[id]` loads by route ID with all settings cards persisting (department, greeting, timezone, working hours, accent, speaking style/speed/tone, country, business hours, call forwarding/routing, knowledge metadata).
3. Done: Dashboard reads owner-scoped `calls`, `appointments`, and `activity_events` through `getDashboardSnapshot` (`lib/dashboard.ts`) with loading/empty/error/retry states.
4. Done: Durable, idempotent WhatsApp inbound processing behind the signed webhook (`lib/whatsappIngest.ts` + server-only `lib/server/whatsappProcessor.ts`). Meta events deduplicate on immutable message IDs via the `webhook_events` ledger, resolve owners through `whatsapp_channels`, store conversations/messages under RLS, and draft replies through `lib/ai/provider.ts` (safe mock; no real API key). Outbound sending stays feature-flagged (`WHATSAPP_OUTBOUND_ENABLED=false`) until Meta registration clears. The baseline, settings/dashboard, and messaging schema is packaged in the canonical `supabase/migrations/*.sql` chain.
5. Done: Real conversation inbox at `/conversations` reads owner-scoped `conversations`/`messages`, masks customer identifiers in the list/header, shows inbound history and blocked AI drafts, and includes loading/empty/error states. The route is protected by both `proxy.ts` and `requireAuthenticatedUser()`.
6. Implement real outbound sending behind the feature flag after Meta registration, plus a controlled end-to-end test with one known-good number.
7. Done: Optional OpenAI Responses API provider is behind the same interface and selected only in `lib/server/aiProvider.ts`. It requires explicit `AI_PROVIDER=openai`, `OPENAI_API_KEY`, and `OPENAI_MODEL`, sends `store: false`, sanitizes failures, and keeps mock as the safe default/fallback. Never log message bodies or customer numbers.
8. Done: Meta delivered/read/failed receipts deduplicate through the ledger and update only the owner&apos;s matching outbound message without status regression. A fail-closed, fixed-batch recovery route at `POST /api/internal/whatsapp/retry` wraps `retryPendingWebhookEvents` behind a separate 32+ character Bearer secret. Next consider moving inline webhook work to a queue/worker using the same processor boundary.
9. Done: Global Next.js security headers block framing and object embedding, prevent MIME sniffing, enforce long-lived HTTPS, restrict referrer/browser capabilities, isolate opener context, and set a non-breaking baseline CSP. All `/api/*` responses are `no-store`; configuration tests prevent accidental removal.
10. Done: WhatsApp webhook request bodies are capped at 1 MiB with both declared-length fast rejection and actual streamed-byte enforcement. Oversized requests receive `413` before signature processing, protecting server memory without changing valid Meta webhook behavior.
11. Done: The public onboarding is explicitly a non-persistent preview. It caps business text at 1,000 characters, keeps answers in memory only, removes the unused local-storage persistence path, and hands off to secure signup instead of claiming a real employee is ready or rendering a fabricated dashboard.
12. Done: OpenAI inputs are bounded, structured as untrusted JSON, and paired with explicit prompt-injection/action-claim/secret-exfiltration guardrails. Webhook claim failures log only a generic message, never event IDs or database error details.
13. Done: Login/signup normalize and bound email input, cap password length, require 12-character signup passwords, keep provider details out of the UI, and correctly stop at an email-confirmation state when Supabase does not create a session.
14. Done: Account recovery now spans `/forgot-password`, one-time PKCE exchange at `/auth/callback`, and authenticated `/reset-password`. Redirects are fixed to the local reset route, feedback prevents account enumeration, new passwords use the same 12–128 character boundary, and the recovery session signs out after success. Configure the production callback URL in Supabase's redirect allowlist.
15. Run RLS integration tests against a dedicated Supabase project (`npm run test:integration`; see `tests/integration/README.md`) to validate all policies end to end.
16. Done in code, rollout-gated: Phase 1 workspace tenancy includes personal workspace bootstrap, membership roles, guarded business-table backfill/cutover, workspace-scoped policies, and `lib/workspaces.ts`. Apply the canonical chain only in its manifest order to an isolated target, record a backup for existing synthetic data, then prove cross-workspace isolation with two accounts before enabling flags.
17. Done, safely locked: The hardcoded Deploy readiness card is now an evidence-based activation checklist. It evaluates stored identity/behavior/voice/knowledge plus real channel/webhook/runtime/outbound configuration. Active remains disabled even when the visible checklist passes because the trusted server writer for fresh `ai_employee_activation_evidence` is not implemented yet.
18. Done in code, rollout-gated: lifecycle transitions and the per-employee automation kill switch are implemented with tests and emergency-pause UI. Canonical migration `20260824000600_employee_lifecycle.sql` follows the cutover; verify it before considering `EMPLOYEE_LIFECYCLE_ENABLED=true`. Legacy Active rows intentionally become Paused; no migration auto-activates an employee.
19. Done in code, rollout-gated: `audit_events` is client-immutable and workspace-readable; a database trigger records lifecycle/kill-switch changes atomically. Verify canonical migration `20260824000700_audit_events.sql` and RLS before considering `AUDIT_LOG_ENABLED=true`.
20. Done in code, rollout-gated: workspace-wide automation pause defaults on, only Owner/Admin can change it, and every change is atomically audited. Verify canonical migration `20260824000800_workspace_kill_switch.sql` and role enforcement before considering `WORKSPACE_SAFETY_ENABLED=true`.
21. Done in code, rollout-gated: protected `/settings/team` lists workspace members and allows role updates under Owner/Admin RLS. A database guard protects membership identity, the final Owner, and Owner-role changes. Verify canonical migration `20260824000900_team_role_management.sql` before considering `TEAM_MANAGEMENT_ENABLED=true`; invitations remain intentionally out of scope for this slice.
22. Done: the first six-agent security cycle removed direct status controls, keeps the workspace kill switch visible during dashboard data failures, blocks lifecycle mutation through generic employee settings, makes the WhatsApp runtime fail closed when the workspace safety flag/state is absent, and requires an explicitly Active, unpaused employee before an AI draft can be generated. Database guards now reject unsafe inserts/direct lifecycle writes, narrow safety changes to approved RPCs, serialize final-owner changes, and retain audit history when workspace deletion is attempted.
23. Done in code, rollout-gated: the workspace foundation/cutover received an independent CIPHER review. It now labels exactly one creator-owned personal workspace, aborts ambiguity or schema/policy drift, locks concurrent signups during bootstrap, never chooses the oldest arbitrary membership, verifies RLS/FKs/indexes/policies, makes `workspace_id` and legacy `user_id` immutable, and removes obsolete conversation uniqueness. App workspace resolvers fail closed on duplicates. Do not apply these migrations live until a backed-up dedicated Supabase project passes the two-account integration plan.
24. Done: GitHub CI is least-privilege and supply-chain pinned, shallow `GET`/`HEAD /api/health` is privacy-safe, and `docs/OPERATIONS_RUNBOOK.md` defines release, smoke, rollback, and incident boundaries. The 2026-08-26 reviewed batch passes lint, typecheck, 95/95 unit tests, production build, and dependency audit with zero vulnerabilities; the live Supabase integration suites remain intentionally unexecuted without dedicated test credentials.
25. Done: The accelerated delivery board assigns safe AI/OpenCode work packets and release gates. `npm run preflight:preview` rejects placeholder/missing Supabase configuration, non-mock AI, partial inbound configuration, weak retry secrets, and enabled rollout/outbound flags without printing values. `npm run smoke:deployment -- https://preview-origin` verifies the public health contract and unauthenticated dashboard boundary; authenticated smoke checks remain manual and synthetic.
26. Done and integrated: `/ai-employees/[id]/test` provides an authenticated, owner-RLS-scoped employee simulation. Its Server Action validates bounded input, constructs `MockAIProvider` directly, returns a bounded draft, and performs no persistence, external provider call, send, activation, or feature-flag change. The UI repeatedly states “Simulation — not sent” and “not saved.” Live two-account RLS execution remains a dedicated-test-project gate, not a completed claim.
27. Done in code, rollout-gated: canonical migration `20260824001000_employee_versions.sql`, `lib/employeeVersions.ts`, and protected `/ai-employees/[id]/versions` provide immutable bounded settings snapshots and role-checked restore. Browser clients have no history write policy; restore preserves the current state, emits an audit event, and leaves lifecycle/channel/safety fields unchanged. Keep `EMPLOYEE_VERSION_HISTORY_ENABLED=false` until the dedicated migration and two-account Viewer/Operator/cross-workspace restore tests pass.
28. Done in code, rollout-gated: canonical migration `20260824001100_knowledge_v0.sql`, `lib/knowledgeEntries.ts`, and protected `/ai-employees/[id]/knowledge` provide structured note/FAQ CRUD with explicit draft/verified state. Composite tenant integrity and role RLS block cross-workspace assignment; database guards freeze identity/actor fields; audit metadata omits content. Verified FAQs can answer deterministically in sandbox/runtime draft paths without an external provider, while drafts never enter AI context. Keep `KNOWLEDGE_V0_ENABLED=false` until dedicated two-account role/RLS tests pass. File ingestion/retrieval remains unbuilt.
29. Done in code, rollout-gated: canonical migration `20260827183015_whatsapp_channel_assignment.sql` adds an explicit, nullable channel-to-employee relationship and strengthens conversation assignment, both protected by `(workspace_id, ai_employee_id)` referential integrity; legacy conversation mismatch aborts migration and assignment audits contain no message content. Existing channels are deliberately not backfilled. The webhook runtime loads only the assigned Active/unpaused employee and synchronizes existing conversation metadata after reassignment; unassigned channels or a false/missing `WHATSAPP_CHANNEL_ASSIGNMENT_ENABLED` retain inbound history but create no AI draft. The employee settings UI can link/reassign channels under existing workspace-admin RLS. Keep the flag false until a dedicated migrated Supabase project passes owner, cross-workspace, deletion/unassignment, and exact-routing tests.
30. Done in code, execution pending: official stable Supabase CLI `2.116.0` is pinned exactly with a committed unlinked local config. `npm run verify:supabase:local` refuses hosted-project links, requires Docker, resets local Postgres twice through the canonical chain, lints the schema, and prints local migration history. This machine currently lacks Docker, so do not claim the reset proof passed. Follow `docs/SUPABASE_LOCAL_TESTING.md`; never replace the guarded verifier with `--linked` or `db push`.
31. Done: pinned Playwright smoke coverage runs one Chromium project against a freshly built local app. It checks the exact privacy-safe health contract, login availability, and the secret-free fail-closed dashboard boundary; artifacts are ignored and screenshots/traces stay off by default. GitHub CI installs Chromium explicitly and runs the smoke after its production build. This does not replace authenticated, RLS, migration, preview, or production smoke evidence.
32. Done in code, activation pending: optional Sentry error monitoring is wired through the official Next.js SDK and stays disabled without a structurally valid public DSN. PII/default data, cookies, headers, bodies, query parameters, user details, database values, AI inputs/outputs, tracing, replay, build telemetry, and source-map upload are explicitly off. Follow `docs/SENTRY_MONITORING.md`; account creation, preview activation, and a synthetic privacy inspection require owner approval.
33. Done in code, rollout-gated: canonical migration `20260829072333_conversation_safety_controls.sql`, `lib/conversationSafety.ts`, the WhatsApp processor, and the protected inbox implement serialized Owner/Admin/Operator human takeover plus durable exact-keyword customer opt-out. Direct browser table writes remain unavailable; the service-role opt-out RPC is not executable by browser roles; audit metadata contains only state/source labels. Human takeover and opt-out block AI drafts immediately, and safety-state read failures fail closed after retaining inbound history. Keep `CONVERSATION_SAFETY_ENABLED=false` until a dedicated migrated Supabase project passes the role, cross-workspace, idempotency, keyword, and runtime tests. No live migration, flag change, outbound send, or Meta change has occurred.

Do not commit `.env.local` or any real secret. Run `npm run check` (lint, typecheck, tests, build) plus `npm audit` and inspect `git diff` before every handoff. Never log message bodies, access tokens, app secrets, signatures, raw payloads, or end-customer phone numbers.
